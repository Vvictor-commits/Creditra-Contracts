name: Cargo Deny

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v1